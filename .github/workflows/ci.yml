name: CI

on:
  push:
    branches: [main, feature]
    paths:
      - 'fe-lab/**'
      - 'vox-task/**'
      - 'eng-chat/**'
  pull_request:
    branches: [main]
    paths:
      - 'fe-lab/**'
      - 'vox-task/**'
      - 'eng-chat/**'

jobs:
  build-fe-lab:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: fe-lab

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: fe-lab/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  build-vox-task:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: vox-task

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: vox-task/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

  build-eng-chat:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: eng-chat

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: eng-chat/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck
