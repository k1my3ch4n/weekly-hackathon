name: CI

on:
  push:
    branches: [main, feature]
    paths:
      - 'fe-lab/**'
      - 'vox-task/**'
      - 'bundle-care/**'
  pull_request:
    branches: [main]
    paths:
      - 'fe-lab/**'
      - 'vox-task/**'
      - 'bundle-care/**'

jobs:
  build-fe-lab:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: fe-lab

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: fe-lab/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  build-vox-task:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: vox-task

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: vox-task/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

  build-bundle-care:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: bundle-care

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: bundle-care/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build report-ui
        run: npm run build:ui

      - name: Build CLI
        run: npm run build:cli

      - name: Test CLI
        run: npm run test --workspace=cli
